# Ensure requirements.txt is always stored and checked out as UTF-8-friendly text (no UTF-16)
requirements.txt text eol=lf

# SQLite must stay byte-identical on all platforms (clone/checkout must not rewrite line endings)
db.sqlite3 binary
